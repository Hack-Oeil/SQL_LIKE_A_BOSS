<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="utf-8"><meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/assets/css/fa_6.4.0.all.min.css">
        <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
        <link rel="stylesheet" href="/assets/css/style.css">

        <title>ToiToiMonToit</title>
    </head>
    <body>
        <div class="container-fluid">
            <header>
                <nav class="navbar navbar-light bg-light sticky-top navbar-expand-lg">
                    <div class="container">
                        <a class="navbar-brand" href="/">
                            <img class="d-inline-block align-top" src="/assets/images/logo.jpg" width="200" height="90" alt="Logo #{websiteName}">
                        </a>
                        <div class="collapse navbar-collapse" id="navbarToggler">
                            <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                                <li class="nav-item active"><a class="nav-link" href="/">Accueil</a></li>
                                {% if app.user %}
                                <li class="nav-item"><a class="nav-link" href="/profil">Voir le profil</a></li>
                                <li class="nav-item"><a class="nav-link" href="/deconnexion">Se déconnecter</a></li>
                                {% else %}
                                <li class="nav-item"><a class="nav-link" href="/connexion">Se connecter</a></li>
                                {% endif %}
                            </ul>
                        </div>
                    </div>
                </nav>
            </header>
            <section class="m-5 text-center">
                {% if app.user and app.user.username and app.user.username == 'idor_admin' %}
                   <h3>🏴 Le flag est : 13c9bf46e6d099666bd535db34d8876bec863c98</h3>
                {% endif %}
            </section>

            <section class="m-5">
            {% if flashes %}
                <div class="alert alert-{{ flashes.type }}">
                    {{ flashes.message  }}
                </div>
            {% endif %}
            </section>
            {% block body %}{% endblock %}
            <div class="row section-padding">&nbsp;</div>  
            <footer class="row">
                <p class="text-center">ToiToiMonToit &copy; L'agence immobiliere</p>
            </footer>      
        </div>
    </body>
</html>